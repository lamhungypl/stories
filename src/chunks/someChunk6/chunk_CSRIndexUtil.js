__d(
  'CSRIndexUtil',
  ['invariant'],
  function (a, b, c, d, e, f, g, h) {
    b = 0;
    function a(a) {
      a.substr(0, 1) === ':' || h(0, 21456, a);
      return a
        .substr(1)
        .split(',')
        .map(function (a) {
          return parseInt(a, 10);
        });
    }
    g.UNKNOWN_RESOURCE_INDEX = b;
    g.parseCSRIndexes = a;
  },
  98,
);
